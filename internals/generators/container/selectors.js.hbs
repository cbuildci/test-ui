import { createSelector } from 'reselect';
import { initialState } from './reducer';

// Direct selector to the {{ camelCase name }} state domain.
const selectDomain =
    (globalState) => globalState.get('{{ camelCase name }}', initialState);

// Default selector used by {{ properCase name }}
export default createSelector(
    selectDomain,
    (domainState) => domainState.toJS(),
);

/*
 * Other specific selectors
 */

// export const selectIsLoading = createSelector(
//     selectDomain,
//     (domainState) => domainState.get('isLoading'),
// );

// export const selectLoadError = createSelector(
//     selectDomain,
//     (domainState) => domainState.get('loadError'),
// );
